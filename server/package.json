{
  "name": "server",
  "private": true,
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:test": "DATABASE_URL=postgresql://helpdesk:helpdesk@localhost:5432/helpdesk_test prisma migrate deploy",
    "db:seed": "bun prisma/seed.ts",
    "db:seed:test": "DATABASE_URL=postgresql://helpdesk:helpdesk@localhost:5432/helpdesk_test bun prisma/seed-e2e.ts"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "better-auth": "^1.7.4",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "prisma": "^7.3.0",
    "typescript": "^5.9.3"
  }
}
