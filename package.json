{
  "name": "helpdesk",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev:client": "bun run --cwd client dev",
    "dev:server": "bun run --cwd server dev",
    "test:e2e": "docker compose up -d postgres && bun run --cwd server db:migrate:test && bun run --cwd server db:seed:test && playwright test",
    "test:e2e:ui": "docker compose up -d postgres && bun run --cwd server db:migrate:test && bun run --cwd server db:seed:test && playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^24.13.3"
  }
}
