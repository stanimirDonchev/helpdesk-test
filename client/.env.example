# Optional: override the API base URL used in production builds.
# In development, the Vite dev server proxies /api to the Express server (see vite.config.ts).
# VITE_API_BASE_URL=https://api.example.com
